<%= form_for(@agency_contact) do |f| %>
  <% if @agency_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agency_contact.errors.count, "error") %> prohibited this agency_contact from being saved:</h2>

      <ul>
      <% @agency_contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :phone %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :location_id %><br>
    <%= f.number_field :location_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
